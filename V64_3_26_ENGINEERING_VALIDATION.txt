V64.3.26 EAF-ICER-SARC engineering validation
================================================

Source basis
------------
- V26 was built from the previously verified V64.3.25 EAF-ICER-DRC delivery that matches the uploaded V25 result provenance.
- The code zip uploaded in the V25-result turn was inspected and is an unrelated OC-RAP v48.56 repository; it was NOT used as the V25 base.

V25 result-protocol repair
--------------------------
- V25 did not TRAIN STOP: aggregate-downside TRAIN gate = 5/5 folds, 71 replacements, +5.527642.
- Fresh A/B 8-arm artifacts are complete and set-identical to their token manifests.
- False STOP cause: launcher required evaluator row order == hash-manifest order.
- Repair: exact manifest set equality + 500 uniqueness + identical emitted order across paired arms.
- Historical V25 launcher and new V26 launcher both contain the repaired contract.
- The repair changes no planner action, metric, risk memory, threshold, or checker criterion.

V26 mechanism implementation
-----------------------------
- Added 10 fixed identity-preserving selected-evidence semantic-family coordinates:
  5 candidate family signed sums + 5 candidate-minus-incumbent family signed sums.
- Families are the existing frozen certificate families:
  feasibility / reachability_interaction / precedence / decision_boundary / dynamic_regularity.
- No magnitude sorting, candidate-specific L1 normalization, learned embedding, family/group weight, density gate, or validation-tuned threshold.
- Added regret_risk_feature_mode=semantic_family_aligned:
  frozen 18-D aggregate evidence features + 10-D semantic-family coordinates = 28-D.
- TRAIN standardization + equal per-dimension metric weight 1/28.
- Downside objective is unchanged: K=32/64, inverse-distance weighting, mean-minus-negative-RMS, multiplier=1, zero boundary, min over scales.
- Frozen support, scalar dominance, incumbent-default operator, scalar ranking, EAF/selector/final guards unchanged.

Instrumentation/provenance
--------------------------
- nuplan_planner passes exact family IDs aligned to selected evidence atoms.
- tournament hard-fails semantic-family runtime mode if selected-family identity is absent/misaligned.
- evaluate_open_loop logs semantic-family coordinates only for deployment-admissible challengers.
- V26 TRAIN fitter streaming schema requires complete 18-D aggregate + 10-D semantic instrumentation on every eligible replacement edge; missing semantic coordinates are a hard STOP, not silently dropped.
- V26 TRAIN replay uses a completion marker so an interrupted partial local frontier is not silently reused.
- Explicit V26_TRAIN_EDGES reuse is allowed but the fitter still hard-validates 3000 TRAIN scenes and full feature coverage.

Fresh identity/data discipline
------------------------------
- Packaged V26 design exclusion contains exactly 6700 unique validation tokens:
  previous 5700 + inspected V25 fresh 1000, with zero overlap.
- V26 fresh selection uses a new hash seed.
- TRAIN/fresh and design/fresh zero-overlap checks are hard STOPs.
- Per split, all four arms must be 500 unique, set-equal to the manifest, and share identical emitted row order.

Tests
-----
1) V26-specific unit tests:
   6/6 PASS
   - semantic family identity preserved;
   - candidate/incumbent correspondence preserved;
   - semantic-family swaps change representation where sorted spectrum cannot;
   - runtime schema exactly 18+10=28;
   - aggregate vs SARC configs differ only in risk representation;
   - equal 1/28 metric weights/no semantic group weight.

2) V64.3.13–V64.3.26 targeted mechanism stack:
   88/88 PASS
   warnings: 2 existing PyTorch Transformer nested_tensor/norm_first warnings.

3) Complete repository, split into three file batches because one monolithic local run exceeded the execution wrapper timeout (no test failure occurred):
   batch 1: 95/95 PASS, 9 warnings
   batch 2: 153/153 PASS, 4 warnings
   batch 3: 170/170 PASS, 23 warnings
   TOTAL: 418/418 PASS
   TOTAL warnings: 36
   warning class: existing PyTorch Transformer nested_tensor/norm_first only; no new warning class.

4) Python compileall:
   PASS

5) bash syntax:
   RUN_V64_3_26_EAF_ICER_SARC_SCREEN_2GPU.sh PASS
   repaired RUN_V64_3_25_EAF_ICER_DRC_SCREEN_2GPU.sh PASS

6) Synthetic memory/config contracts:
   aggregate-downside 18-D contract PASS
   semantic-family-downside 28-D contract PASS
   verified: memory SHA, exact feature names, equal metric weights, K32/64,
   downside multiplier 1, certificate kind, replacement-only risk, frozen V20 heads.

Scientific effect validation
----------------------------
- No V26 effect result is claimed from the local environment because the server nuPlan GPU/cache is not available here.
- V26 must first pass the fixed 3000-TRAIN crossfit gate. Only then may the launcher consume a new untouched A/B 1000 validation scenes.
- TRAIN fail or either fresh block fail remains fail-closed; no pooled rescue or hyperparameter tuning is implemented.
