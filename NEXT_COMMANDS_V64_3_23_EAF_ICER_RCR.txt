# V64.3.23 EAF-ICER-RCR next command
# Main: evidence-local multiscale regret coherence + self-consistent signed-evidence ranking.
# Transition-local RCR is a controlled ablation only; it cannot rescue a failed evidence-local main.
# Run exactly this screen. Do not tune zero boundary, K={32,64}, one-SE multiplier,
# transition group weights, signed-profile weights, B/M, certificates or guards.
# If the V22 output directory is present, its completed TRAIN transition frontier is reused automatically.
# If it is absent, V23 will reproduce the frozen 3000-scene TRAIN instrumentation once.

bash RUN_V64_3_23_EAF_ICER_RCR_SCREEN_2GPU.sh

# STOP discipline:
# - TRAIN fit STOP: do not bypass the evidence-local 5-fold selected-path gate.
# - A or B fresh STOP: do not pool the blocks or tune local-risk parameters.
# - If scalar succeeds but signed main fails: keep scalar as the next candidate; do not tune view weights.
# - If evidence-local main succeeds but transition ablation fails: do not tune transition weights; keep transition out of the main.
# - PASS of both A/B: only one independent frozen full-val reproduction is allowed next.
# - test / closed-loop remain forbidden until that independent reproduction passes.
