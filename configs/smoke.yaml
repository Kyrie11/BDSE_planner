# Fast smoke-test overrides; do not use for paper experiments.
runtime:
  include_drivable_polygons: false
  max_drivable_polygons: 4
  max_polygon_points: 32
candidate:
  valid_requires_dynamic: false
teacher:
  robust:
    enabled: false
  cost_eval_stride: 2
training:
  batch_size: 4
  epochs: 1

calibration:
  epsilon_cal: 0.0
