V64.3.50.1 EAF-ICER-PIOR — engineering repair rerun

Scientific status of the uploaded V50 result:
- ENGINEERING/DATA STOP, not a PIOR algorithm result.
- Do NOT use the 50/64 fired subset for PIOR AUC/retention analysis.
- Do NOT run untouched/final validation yet.

Recommended clean rerun (new output root is now the launcher default):

  cd bdse_v64_3_50_eaf_icer_pior
  bash RUN_V64_3_50_EAF_ICER_PIOR_TRAIN_2GPU.sh

Default output root:

  ../outputs_v64_3_50_1_eaf_icer_pior_train_2gpu_v1

The old failed root may be kept for audit; do not copy its batch_0000 into the new root.

Default engineering controls:

  WORKERS_PER_ARM=4
  PIOR_FIRST_BATCH_SIZE=4
  PIOR_BATCH_SIZE=64
  PIOR_HEARTBEAT_SECONDS=30
  PIOR_RESUME=1
  PIOR_SERIALIZE_GPU_INFERENCE=0
  PIOR_PROFILE_CLOSED_LOOP=1

The first control/treatment batch is a paired fail-fast preflight. Both 4-scene batches must certify:
- exact scenario token;
- iteration index 0;
- target source = preregistered_V49_manifest_iteration0_proposal;
- proposal action = manifest full_selected_action;
- treatment executes proposal, control executes incumbent;
- exactly one event per token;
- no rerank / second-best / fallback;
- valid per-scenario official metrics.

Only after BOTH arms pass this preflight does the runner continue the expensive remaining TRAIN collection.

Explicit server paths if needed:

  V49_ROOT=/path/to/outputs_v64_3_49_eaf_icer_siir_screen_2gpu_v1 \
  BDSE_TRAIN_CACHE=/data0/senzeyu2/dataset/nuplan/data/cache/bdse_train_v2 \
  NUPLAN_ROOT=/data0/senzeyu2/dataset/CapPlan/data/nuplan \
  NUPLAN_DB_SPLIT_ROOT=/data0/senzeyu2/dataset/CapPlan/data/nuplan/nuplan-v1.1/splits \
  EAF_V64_3_13_ROOT=/path/to/outputs_v64_3_13_eaf_dmvr_screen_2gpu_v1 \
  GPU0=0 GPU1=1 \
  bash RUN_V64_3_50_EAF_ICER_PIOR_TRAIN_2GPU.sh

If checkpoint auto-resolution is unavailable:

  export EAF_CKPT=/path/to/bdse_v64_saqa_bcc.epoch_XXXX.pt

Useful early inspection after the preflight starts:

  OUT=../outputs_v64_3_50_1_eaf_icer_pior_train_2gpu_v1
  grep -E '\[PIOR-PREFLIGHT|\[PIOR-BATCH|\[PIOR-TICK' "$OUT/logs/v64_3_50_paired_closed_loop.out" | tail -n 50

Expected success messages before full collection:

  [PIOR-PREFLIGHT-PASS] arm=control scenarios=4 ...
  [PIOR-PREFLIGHT-PASS] arm=treatment scenarios=4 ...
  [PIOR-PREFLIGHT-PAIR-PASS] arm=control ...
  [PIOR-PREFLIGHT-PAIR-PASS] arm=treatment ...

If preflight fails, send:
- the traceback;
- both batch_0000/run.log files;
- both batch_0000/pior_probe_events.jsonl files;
- both batch_0000/pior_probe_targets.json files.
Do not continue to the 502-scene collection manually.

Scientific execution discipline:
- paired TRAIN collection complete -> run nested PIOR identification + causal-retention gate automatically;
- TRAIN gate FAIL -> scientific STOP; no untouched closed-loop validation and no threshold/feature/loss rescue;
- TRAIN gate PASS -> freeze PIOR; only then design/run untouched paired closed-loop validation.
