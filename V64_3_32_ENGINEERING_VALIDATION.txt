V64.3.32 EAF-ICER-SSIR engineering validation
============================================

Uploaded V64.3.31 result audit
------------------------------
- Result STOP classification: preregistered TRAIN scientific gate, not runtime/engineering crash.
- Uploaded V31 code compile: PASS.
- V31 focused tests: 5/5 PASS.
- Uploaded V31 full repository regression: 462/462 PASS, 36 pre-existing PyTorch Transformer warnings.
- V31 launcher bash -n: PASS.
- No fitter/runtime feature/target mismatch found.
- Scientific scope: reliable development-level attribution of mean-ranking proposal component only; CAL500/A/B were intentionally not spent.

V64.3.32 implementation checks
------------------------------
- python -m compileall -q bdse: PASS.
- RUN_V64_3_32_EAF_ICER_SSIR_SCREEN_2GPU.sh bash -n: PASS.
- V32 focused tests: 6/6 PASS.
  * candidate-specific simultaneous lower bound can reorder a higher-mean/high-leverage candidate behind a lower-mean/lower-leverage candidate;
  * no positive LCB returns incumbent with no fallback;
  * all-flagged structural domain delegates exactly;
  * malformed leverage schema fails closed;
  * V31 artifact without leverage matrix remains unit-scale backward compatible;
  * independent CAL500 calibrator uses exactly one score per direct-eligible scene and excludes unrelated/no-direct scenes from risk calibration.
- V13–V32 targeted regression: 138/138 PASS, 2 pre-existing Transformer warnings.
- Full repository regression: 468/468 PASS, 36 warnings.
- Warning classes: no new class; all warnings are the historical PyTorch Transformer nested_tensor/norm_first warning.

Protocol checks encoded in launcher/tools
-----------------------------------------
- Exact uploaded V31 failure is a hard prerequisite before V32 proceeds:
  0/5 fold-safe, 612 selected, 342 positive, sum -22.073251969552103, worst -4.041263536178889, capture 0.5958188153310104.
- V31 consumed no CAL/fresh scenes; V32 retains the exact 10700-token design exclusion SHA256
  041ec824756777576391756ef3721617459bb0c0a45f7f43226b52254d951473
  and uses a new label-free fresh seed.
- TRAIN gate evaluates the complete mechanism with rotating 3-fold fit / 1-fold calibration / 1-fold test separation.
- TRAIN fit tool writes a token-level scene audit CSV before any scientific STOP.
- Independent CAL500 fixes one alpha=0.05 direct-domain scene-simultaneous quantile; at least 64 direct-eligible scenes required.
- A/B remain independent 500-scene promotion blocks; no pooling rescue.
- Main runtime selects by positive candidate-specific LCB before extremal winner selection; no second-best fallback.
- Query budget, B/M, acquisition, EAF, support, structural and final execution contracts remain frozen.
- No alpha/ridge/scale/support/threshold sweep exists in the V32 launcher.

Local limitation
----------------
The exact V30.2 frozen TRAIN edge artifact referenced by the server launcher is not bundled in the uploaded V31 result archive, so the new V32 nested TRAIN numerical result cannot be fabricated locally. The server launcher must produce the first real SSIR mechanism result. This limitation does not affect compilation/regression validation or the attribution of the uploaded V31 0/5 aggregate gate.
