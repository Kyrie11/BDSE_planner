V64.3.43 EAF-ICER-CFRV next command
=====================================

1) Unpack / enter the delivered code directory.

2) Main preregistered 2-GPU experiment:

cd bdse_v64_3_43_eaf_icer_cfrv
bash RUN_V64_3_43_EAF_ICER_CFRV_SCREEN_2GPU.sh

The launcher first requires the exact uploaded V42 TRAIN failure signature and verifies that V42 did not consume CAL500/A500/B500. It then replays the same frozen 3000 TRAIN token list with the new runtime-only counterfactual future-response observables.

If your historical roots/caches are elsewhere:

cd bdse_v64_3_43_eaf_icer_cfrv
V42_ROOT=/path/to/outputs_v64_3_42_eaf_icer_ovdr_screen_2gpu_v1 \
EAF_V64_3_13_ROOT=/path/to/outputs_v64_3_13_eaf_dmvr_screen_2gpu_v1 \
BDSE_TRAIN_CACHE=/path/to/bdse_train_v2 \
BDSE_VAL_CACHE=/path/to/bdse_val_v2 \
bash RUN_V64_3_43_EAF_ICER_CFRV_SCREEN_2GPU.sh

If checkpoint auto-resolution is not available, add:

EAF_CKPT=/path/to/checkpoint.pt

Expected protocol:
- hard reproduce V42 RSMR and QUALITY TRAIN signatures;
- nested 5-fold TRAIN causal arms: QUALITY-CONTROL / QUALITY+FUTURE-MEAN / QUALITY+FUTURE-ROBUST / CFRV-MAIN;
- if TRAIN gate fails: STOP before fresh selection;
- only if TRAIN gate passes: select new independent CAL500+A500+B500 with seed
  v64.3.43-eaf-icer-cfrv-cal500-double-fresh-v1
  and run the double-fresh screen.

Do NOT run/tune official closed-loop before TRAIN + double-fresh promotion. Do NOT change threshold, lambda, response-mode probabilities, CVaR alpha/weight, top-K, candidate count, or value cutoffs after seeing V43 TRAIN results.
