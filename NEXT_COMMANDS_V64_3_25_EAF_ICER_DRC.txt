V64.3.25 EAF-ICER-DRC — next command
========================================

Scientific branch decision from uploaded V64.3.24 TRAIN result:
- KEEP: aggregate evidence-local downside-RMS regret certificate (5/5 fixed TRAIN folds safe).
- CONTROL: aggregate evidence-local mean-SE certificate.
- DROP from fresh screen: full attribution-resolved spectrum (3/5 fixed TRAIN folds safe; do not tune its metric weights).
- KEEP FROZEN: EAF acquisition/frontier, B<=16 evidence interface, DARM-anchor frontier, support head, scalar dominance head, final guard, K={32,64}, downside multiplier=1, zero boundary.

Primary command:

  bash RUN_V64_3_25_EAF_ICER_DRC_SCREEN_2GPU.sh

The launcher first tries to reuse the already-computed V24 3000-scene TRAIN frontier. If your V24 output lives elsewhere, avoid a redundant TRAIN replay by pointing to it explicitly:

  V24_TRAIN_EDGES=/absolute/path/to/outputs_v64_3_24_eaf_icer_arc_screen_2gpu_v1/provenance/train_v20_with_full_attribution_spectrum_edges.jsonl \
  bash RUN_V64_3_25_EAF_ICER_DRC_SCREEN_2GPU.sh

Fail-closed rules:
- TRAIN gate: aggregate-downside must be selected-path safe in all 5 fixed scene folds, select >=64 replacements, and have total TRAIN teacher-improvement >=0. Do not bypass.
- Fresh: A and B are disjoint untouched 500-scene blocks; each must independently pass. No pooled rescue.
- Do not tune K, downside multiplier, zero boundary, support/dominance thresholds, B/M, selector/acquisition, or evidence-interface weights on fresh validation.
- If DRC is path-safe but not incremental over mean-SE, do not claim downside-certification novelty and do not tune the multiplier.
- If DRC fails the selected tail on fresh, next analysis should test whether the frozen aggregate evidence interface misses a semantic latent state; only then consider a structured atom-identity representation. Do not return to abs-sorted/L1-normalized full spectra.
- Passing this screen permits only one frozen independent full-validation reproduction. Test and closed-loop remain forbidden until that reproduction succeeds.
