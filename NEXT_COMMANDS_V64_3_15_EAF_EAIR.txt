V64.3.15 EAF-EAIR — NEXT COMMANDS
==================================

Purpose
-------
Run ONLY the small selective reliability/readout screen. Do not run full/test/closed-loop yet.
V64.3.15 freezes acquisition, B=16, M=24, DARM, DBR and the V64.3.13 EAF checkpoint.
The only fitted parameters are a TRAIN-only standardized logistic reliability readout.

1) Enter the code root
----------------------
cd /path/to/bdse_v64_3_15_eaf_eair

2) Point to the existing datasets
---------------------------------
export BDSE_TRAIN_CACHE=/data0/senzeyu2/dataset/nuplan/data/cache/bdse_train_v2
export BDSE_VAL_CACHE=/data0/senzeyu2/dataset/nuplan/data/cache/bdse_val_v2

3) Point to the original V64.3.13 EAF-DMVR screen/checkpoint root
------------------------------------------------------------------
# Default expected layout:
export EAF_V64_3_13_ROOT=outputs_v64_3_13_eaf_dmvr_screen_2gpu_v1

# If the selected V64.3.13 checkpoint is elsewhere, set it explicitly:
# export EAF_CKPT=/absolute/path/to/the/causally_valid/v64_3_13_checkpoint.pt

# The launcher re-audits the V64.3.13 train log before using the checkpoint.
# If the train log is elsewhere:
# export EAF_TRAIN_LOG=/absolute/path/to/bdse_v64_saqa_bcc.train_log.jsonl

4) Optional screen-size/GPU overrides
-------------------------------------
export GPU0=0
export GPU1=1
export TRAIN_SCENARIOS=3000
export VAL_DISCOVERY_SCENARIOS=1200
export VAL_SCREEN_SCENARIOS=500
export OUT_ROOT=outputs_v64_3_15_eaf_eair_screen_2gpu_v1

# IMPORTANT: the packaged exclusion file contains all 500 V64.3.14 design scenes.
# Do not replace it with a subset.
export DESIGN_EXCLUDE_TOKENS=bdse/configs/v64_3_15_design_exclude_v64_3_14_tokens.txt

5) Run the causal screen
------------------------
bash RUN_V64_3_15_EAF_EAIR_SCREEN_2GPU.sh 2>&1 | tee v64_3_15_eaf_eair_screen_driver.log

6) Interpret the terminal result
--------------------------------
A) STOP SCREEN: EAIR not promoted
   Do NOT run full/test/closed-loop.
   Read:
     $OUT_ROOT/provenance/v64_3_15_eaf_eair_screen.json
   Follow its exact `next_action`.

   Expected causal branches:
   - query_conditioned_action_evidence_representation_adapter
     => reliability information is not recoverable by the small frozen readout.
   - query_conditioned_reliability_representation_not_more_scalar_gate_tuning
     => AUC exists, but scalar reliability does not preserve decisions; do NOT tune threshold.
   - audit_value_ranking_top_challenger_after_reliable_gate
     => reliability works, but the remaining failure is extremal challenger value/ranking.

B) PASS SCREEN
   Still do NOT run test/closed-loop from this launcher.
   The next step is a separate full-val reproduction on a pre-registered split/config.

Forbidden before a PASS + independent full-val reproduction
------------------------------------------------------------
- test set
- closed-loop simulation
- changing B=16 or M=24
- reopening BTP/RET/CET/acquisition
- OCFI alpha/radius sweeps
- EAIR threshold sweeps
- relaxing the evidence-certificate gate
- reusing the V64.3.14 500 design scenes as promotion data
