V64.3.38 EAF-ICER-DAVR engineering validation

Python compile: PASS
Launcher bash -n: PASS
V38 focused: 6/6 PASS
V13-V38 targeted: 178/178 PASS
  historical V13-V37 targeted + new V38 focused in one fixed invocation
Full repository: 508/508 PASS
  deterministic exhaustive test-file partition over all 113 test_*.py files:
  78/78 + 97/97 + 166/166 + 167/167
Warnings: 36 historical / 0 new warning class

Important execution note:
- A one-shot full pytest invocation reached approximately 98% progress but the outer 120-second tool timeout occurred before pytest returned its final exit status. It is NOT counted as a pass.
- The 508/508 full-repository result comes only from four disjoint, exhaustive, sorted contiguous test-file partitions, each returning exit code 0.

V38-specific contracts tested/audited:
- repaired V32.1 scene-equal dense ridge is invariant to duplicating candidate rows inside one scene (scene total loss mass remains one);
- dense cardinal head is distinct from and cannot alter frozen RSMR winner identity;
- dense raw value and dense selected-policy affine runtime modes are deterministic and signed;
- selected-policy DAVR recalibration is one-dimensional and preserves frozen RSMR/dense parameters;
- malformed dense-value schema fails closed;
- V36 scene reservation and V37/V38 post-selection value are mutually exclusive;
- runtime post-selection topology can only accept the exact frozen RSMR proposal or return incumbent; no re-ranking/second-best path exists;
- V38 prerequisite launcher hard-replays the exact V37 failure signature and refuses any previously spent V37 CAL/fresh population;
- V38 fresh split diagnostics use the actual final selected action after value veto.
